# Derived Burger Base Mesh

`burger_base_without_top.stl` is derived from the Apache-2.0 licensed
TurtleBot3 `burger_base.stl` distributed by ROBOTIS.

Triangles extending above 94.01 mm are removed so the lower three Burger
platforms remain while the original top lidar platform and its supports are
omitted.
